# 2026-03-29 主线交付摘要

本次主线交付聚焦已完成功能的稳定落地与交付质量基线确认，不包含新的功能承诺。

## 已交付内容

- 已支持通过 RSSHub 接入微信公众号数据源，补齐微信来源采集能力。
- 线索列表已支持当前页批量操作，并补充前端回归测试覆盖对应交互。
- 已修复线索页首次进入时关键词未自动加载的问题，恢复初始筛选体验。
- 已加强数据处理与数据源类型相关回归测试，覆盖富化与来源类型关键路径。
- 项目文档已整理归档到 `docs/` 与 `docs/superpowers/`，便于后续维护与交付查阅。

## 最低验证命令

后端：

```bash
cd backend && python3 -m pytest
```

前端：

```bash
cd web && npm test
```
